name: Third
on: 
  push:
    branches:
      - thirdday

jobs:

  job1:
    name: 'Trabajo 1'
    runs-on: ubuntu-latest
    steps: 
      - name: 'Checkout repo de jmoyano/eati2023@main'
        uses: actions/checkout@v3
        with:
          repository: jmoyano/eati2023
          ref: main
          path: ./main
        
      - name: Ejecuto cat
        run: ls main

      - name: 'Checkout repo de jmoyano/eati2023@anotherbranch'
        uses: actions/checkout@v3
        with:
          repository: jmoyano/eati2023
          ref: anotherbranch
          path: ./anotherbranch

      - name: Ejecuto cat
        run: ls anotherbranch








